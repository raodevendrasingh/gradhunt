[phases.setup]
commands = [
  "cd backend",
  "pip install -r requirements.txt"
]

[phases.build]
commands = [
  "cd backend",
  "python manage.py migrate",
  "python manage.py collectstatic --noinput"
]

[phases.start]
commands = [
  "cd backend",
  "gunicorn gradHunt.wsgi:application"
]